{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["chat"],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:default",
    "shell:allow-open"
  ]
}
