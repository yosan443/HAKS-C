{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["vision"],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:default",
    "shell:allow-open"
  ]
}
