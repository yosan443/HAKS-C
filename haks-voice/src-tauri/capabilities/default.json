{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["voice"],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:default",
    "shell:allow-open"
  ]
}
